node_modules/
lib/
coverage/
*.tsbuildinfo
.DS_Store
*.log
.pnpm-store/
*.tgz
# Throwaway DSH_HOME used to smoke-test a packed artifact without touching a
# real profile (see the README's install section).
.dsh-verify/
